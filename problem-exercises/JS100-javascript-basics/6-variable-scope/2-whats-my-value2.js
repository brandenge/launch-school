console.log(greeting);

let greeting = 'Hello world!';

/*
This will throw an ReferenceError for referencing an uninitialized variable because a let variable is not hoisted, and therefore is not initialized before the code runs (before run-time, at compile-time).
*/
