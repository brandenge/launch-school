'8' == 8; // => true because JavaScript performs implicit type coercion with the loose equality operator.
'8' === 8; // => false because JavaScript will not perform any type conversion with the strict equality operator.
