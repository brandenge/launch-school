/*
Referencing an out-of-index element in an array will return undefined.

Note that just referencing it, does not add anything to that index, that would requiree assignment, and would then produce gaps in the array.
*/
