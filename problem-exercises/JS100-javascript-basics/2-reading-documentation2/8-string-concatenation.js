/*
1) + is the string concatenation operator.
2) += can be used as well.
3) String.prototype.concat() method (not recommended to use).
4) String interpolation with template literal string syntax using back ticks and ${}.
*/
