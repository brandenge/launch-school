/*
What does the following function do?

function doSomething(string) {
  return string.split(' ').reverse().map((value) => value.length);
}

It takes a string and then splits it into an array based on any spaces
(spaces are the delimiting character), then reverses the array of strings,
and returns a new array that contains the lengths of those strings.
*/
